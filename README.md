```
yarn create hono my-blog-rsc
* selected nodejs template because I am doing this to learn rsc and I thought that template would allow me to focus on rsc
* said installing dependencies was fine
* yarn as my package manager was fine

* `git init` so I can save this on Git

npm install
npm run dev
```

```
open http://localhost:3000
```


Things learned:

